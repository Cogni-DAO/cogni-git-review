id: goal-alignment
schema_version: "0.1"
blocking: true

evaluation-statement: Does NOT Re-implementing mature OSS tools or libraries.

# Unused for MVP. provider assumes this
rule-schema-id: statement-alignment-evaluation
# Present, but totally unused for now.
variables: [pr_title, pr_body, diff_summary]

success_criteria:
  metric: score
  threshold: 0.7
