{
  "description": "Parser preset for Ruff JSON output format",
  "root": "$[*]",
  "fields": {
    "file": "$.filename",
    "line": "$.location.row",
    "column": "$.location.column", 
    "code": "$.code",
    "message": "$.message",
    "severity": "$.level"
  },
  "notes": [
    "Ruff JSON format is array of violation objects",
    "level: typically 'error' or 'warning'",
    "Use with: ruff check --output-format json . > ruff-report.json"
  ]
}